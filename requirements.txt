streamlit# requirements.txt (안정성 검증 조합)

# 기본 라이브러리
streamlit>=1.36
pandas>=2.2
numpy==1.26.4
plotly>=5.22
toml

# STT 및 AI 분석
faster-whisper>=1.0.0
ctranslate2>=4.3
google-generativeai>=0.7.2

# --- 화자 분리 안정성 조합 ---
# huggingface_hub는 pyannote와 torch의 호환성을 위해 버전을 명시합니다.
huggingface_hub==0.23.0

# pyannote.audio 3.1.1과 호환성이 검증된 torch/torchaudio 버전
torch==2.1.2
torchaudio==2.1.2
pyannote.audio==3.1.1

# pyannote가 내부적으로 사용하는 라이브러리들
pytorch-lightning
omegaconf